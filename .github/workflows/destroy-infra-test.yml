name: destroy-infra-test

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  TF_VERSION: "1.6.6"
  TF_LOG: "DEBUG"
  TF_LOG_PATH: "terraform.log"

jobs:
  terraform-destroy:
    name: Terraform Destroy en Test
    runs-on: ubuntu-latest
    environment: test

    defaults:
      run:
        working-directory: terraform

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Terraform init
        run: terraform init -backend-config=../backends/test.hcl

      - name: Terraform destroy (test)
        run: terraform destroy -var-file=../environments/test.tfvars -auto-approve

      - name: Upload destroy logs
        uses: actions/upload-artifact@v4
        with:
          name: terraform-destroy-logs-test
          path: terraform/terraform.log
